import React from 'react';

export default function AuthButton() {
    // Placeholder auth button – replace with real auth flow later
    return (
        <button className="btn-green">
            Log In
        </button>
    );
}
